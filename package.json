{
  "name": "calomentor",
  "version": "1.0.0",
  "description": "Calomentor app",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "web-dev": "lerna run dev --scope=web"
  }
}
