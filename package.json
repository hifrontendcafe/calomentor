{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "web-dev": "lerna run dev --scope=client",
    "server-dev": "lerna run dev --scope=client",
    "dev": "lerna run dev --scope={client, server}"
  },
  "devDependencies": {
    "@types/node": "^14.14.41",
    "@types/react": "^17.0.3",
    "autoprefixer": "^10.2.5",
    "lerna": "^4.0.0",
    "postcss": "^8.2.12",
    "tailwindcss": "^2.1.2",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "client": "file:packages/client"
  }
}
